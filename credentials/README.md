# credentials/

Coloque aqui o arquivo `service-account.json` baixado do Google Cloud Console.

Este diretório é protegido pelo `.gitignore` — arquivos `.json` aqui nunca são commitados.

Veja o passo-a-passo de como gerar esse arquivo no [README.md](../README.md) da raiz do projeto.
